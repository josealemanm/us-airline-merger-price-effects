# Pinned to the versions the reported results were produced with.
numpy==2.5.3
pandas==3.0.6
scipy==1.18.1
statsmodels==0.15.0
duckdb==1.5.5
pyarrow==25.0.1
matplotlib==3.11.2
requests==2.34.2

# Test-only. linearmodels is never imported by the pipeline; it is the
# independent implementation the IV estimator is checked against.
pytest==9.1.1
linearmodels==7.0
